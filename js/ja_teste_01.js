console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}


console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo1 = new Inimigo('Orc', 50, 10);
const inimigo2 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
console.log("Olá");

function validarEmail(email) {
    const regex = /\S+@\S+\.\S+/;
    return regex.test(email);
}
function gerarNumeroAleatorio(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
function formatarData(data) {
    return new Date(data).toLocaleDateString('pt-BR');
}
function esconderElemento(id) {
    document.getElementById(id).style.display = 'none';
}
function mostrarElemento(id) {
    document.getElementById(id).style.display = 'block';
}
function removerItem(array, item) {
    const index = array.indexOf(item);
    if (index !== -1) {
        array.splice(index, 1);
    }
}
function ordenarPorPropriedade(array, propriedade) {
    return array.sort((a, b) => a[propriedade] - b[propriedade]);
}
const numero = parseInt('123');
const decimal = parseFloat('123.45');
function validarInteiro(numero) {
    return Number.isInteger(numero);
}
function validarDecimal(numero) {
    return !isNaN(numero) && numero % 1 !== 0;
}
function objetoVazio(objeto) {
    return Object.keys(objeto).length === 0;
}
function copiarParaAreaTransferencia(texto) {
    navigator.clipboard.writeText(texto);
}
// Definindo uma classe para representar uma tarefa
class Tarefa {
    constructor(descricao, concluida = false) {
        this.descricao = descricao;
        this.concluida = concluida;
    }
}

// Definindo uma classe para representar o sistema de gerenciamento de tarefas
class GerenciadorTarefas {
    constructor() {
        this.tarefas = [];
    }

    // Método para adicionar uma nova tarefa
    adicionarTarefa(descricao) {
        const novaTarefa = new Tarefa(descricao);
        this.tarefas.push(novaTarefa);
    }

    // Método para remover uma tarefa
    removerTarefa(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas.splice(indice, 1);
        }
    }

    // Método para marcar uma tarefa como concluída
    marcarComoConcluida(indice) {
        if (indice >= 0 && indice < this.tarefas.length) {
            this.tarefas[indice].concluida = true;
        }
    }

    // Método para listar todas as tarefas
    listarTarefas() {
        this.tarefas.forEach((tarefa, indice) => {
            console.log(`${indice + 1}. [${tarefa.concluida ? 'X' : ' '}] ${tarefa.descricao}`);
        });
    }
}

// Exemplo de uso do sistema de gerenciamento de tarefas
const gerenciador1 = new GerenciadorTarefas();

gerenciador.adicionarTarefa('Estudar JavaScript');
gerenciador.adicionarTarefa('Fazer compras');
gerenciador.adicionarTarefa('Ler um livro');

console.log('--- Tarefas Pendentes ---');
gerenciador.listarTarefas();

gerenciador.marcarComoConcluida(1);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();

gerenciador.removerTarefa(0);

console.log('\n--- Tarefas Atualizadas ---');
gerenciador.listarTarefas();
// Definição das classes de personagem
class Personagem {
    constructor(nome, classe, hp, atk) {
        this.nome = nome;
        this.classe = classe;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Definição dos inimigos
class Inimigo {
    constructor(nome, hp, atk) {
        this.nome = nome;
        this.hp = hp;
        this.atk = atk;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * (this.atk / 2)) + (this.atk / 2);
        alvo.hp -= dano;
        console.log(`${this.nome} ataca ${alvo.nome} e causa ${dano} de dano.`);
    }
}

// Função para simular uma batalha
function batalhar(jogador, inimigo) {
    console.log(`Um ${inimigo.nome} apareceu!`);
    console.log(`Começa a batalha entre ${jogador.nome} e ${inimigo.nome}.`);
    
    while (jogador.hp > 0 && inimigo.hp > 0) {
        jogador.atacar(inimigo);
        if (inimigo.hp <= 0) {
            console.log(`${inimigo.nome} foi derrotado!`);
            break;
        }
        inimigo.atacar(jogador);
        if (jogador.hp <= 0) {
            console.log(`${jogador.nome} foi derrotado!`);
            break;
        }
    }
}

// Criando instâncias de personagens e inimigos
const jogador1 = new Personagem('Aragorn', 'Guerreiro', 100, 20);
const inimigo11 = new Inimigo('Orc', 50, 10);
const inimigo21 = new Inimigo('Esqueleto', 30, 15);

// Batalhando contra os inimigos
batalhar(jogador, inimigo1);
batalhar(jogador, inimigo2);
class Personagem {
    constructor(nome, vida, ataque) {
        this.nome = nome;
        this.vida = vida;
        this.ataque = ataque;
    }

    atacar(alvo) {
        const dano = Math.floor(Math.random() * this.ataque) + 1;
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} e causou ${dano} de dano.`;
    }
}

function iniciarJogo() {
    const jogador = new Personagem('Herói', 100, 10);
    const inimigo = new Personagem('Inimigo', 80, 8);

    document.getElementById('nomePersonagem').textContent = jogador.nome;
    document.getElementById('vidaPersonagem').textContent = jogador.vida;
    document.getElementById('ataquePersonagem').textContent = jogador.ataque;

    document.getElementById('nomeInimigo').textContent = inimigo.nome;
    document.getElementById('vidaInimigo').textContent = inimigo.vida;
    document.getElementById('ataqueInimigo').textContent = inimigo.ataque;

    const log = document.getElementById('log');
    log.innerHTML = '';

    while (jogador.vida > 0 && inimigo.vida > 0) {
        const turnoJogador = jogador.atacar(inimigo);
        log.innerHTML += `<p>${turnoJogador}</p>`;
        const turnoInimigo = inimigo.atacar(jogador);
        log.innerHTML += `<p>${turnoInimigo}</p>`;

        document.getElementById('vidaPersonagem').textContent = jogador.vida;
        document.getElementById('vidaInimigo').textContent = inimigo.vida;
    }

    if (jogador.vida <= 0) {
        log.innerHTML += `<p>${jogador.nome} foi derrotado!</p>`;
    } else {
        log.innerHTML += `<p>${inimigo.nome} foi derrotado!</p>`;
    }
}
// Definição da classe Personagem
class Personagem {
    constructor(nome, classe, vida, ataque, defesa) {
        this.nome = nome;
        this.classe = classe;
        this.vida = vida;
        this.ataque = ataque;
        this.defesa = defesa;
    }

    // Método para atacar outro personagem
    atacar(alvo) {
        const dano = Math.max(0, this.ataque - alvo.defesa);
        alvo.vida -= dano;
        return `${this.nome} atacou ${alvo.nome} causando ${dano} de dano.`;
    }

    // Método para verificar se o personagem está vivo
    estaVivo() {
        return this.vida > 0;
    }
}

// Definição da classe Jogador que herda de Personagem
class Jogador extends Personagem {
    constructor(nome, classe) {
        let vida, ataque, defesa;
        switch (classe) {
            case 'Guerreiro':
                vida = 150;
                ataque = 15;
                defesa = 10;
                break;
            case 'Mago':
                vida = 100;
                ataque = 20;
                defesa = 5;
                break;
            case 'Arqueiro':
                vida = 120;
                ataque = 18;
                defesa = 8;
                break;
            default:
                throw new Error('Classe de jogador inválida.');
        }
        super(nome, classe, vida, ataque, defesa);
    }
}

// Definição da classe Inimigo que herda de Personagem
class Inimigo extends Personagem {
    constructor(nome) {
        const vida = Math.floor(Math.random() * 100) + 100;
        const ataque = Math.floor(Math.random() * 10) + 10;
        const defesa = Math.floor(Math.random() * 10) + 5;
        super(nome, 'Inimigo', vida, ataque, defesa);
    }
}

// Função para iniciar o jogo
function iniciarJogo() {
    const nomeJogador = prompt('Digite o nome do seu personagem:');
    const classeJogador = prompt('Escolha a classe do seu personagem (Guerreiro, Mago ou Arqueiro):');
    const jogador = new Jogador(nomeJogador, classeJogador);

    alert(`Bem-vindo, ${jogador.nome} o ${jogador.classe}!`);

    let numInimigosDerrotados = 0;

    while (jogador.estaVivo()) {
        const inimigo = new Inimigo(`Inimigo ${numInimigosDerrotados + 1}`);
        alert(`Um ${inimigo.nome} apareceu!`);

        while (jogador.estaVivo() && inimigo.estaVivo()) {
            const opcoes = ['Atacar', 'Fugir'];
            const escolha = prompt('Escolha sua ação:\n1. Atacar\n2. Fugir');
            const indiceEscolha = parseInt(escolha) - 1;

            if (opcoes[indiceEscolha] === 'Atacar') {
                const logAtaqueJogador = jogador.atacar(inimigo);
                const logAtaqueInimigo = inimigo.atacar(jogador);
                alert(logAtaqueJogador + '\n' + logAtaqueInimigo);
            } else if (opcoes[indiceEscolha] === 'Fugir') {
                alert('Você fugiu da batalha!');
                break;
            } else {
                alert('Opção inválida, tente novamente.');
            }
        }

        if (!jogador.estaVivo()) {
            alert('Você foi derrotado. GAME OVER!');
            break;
        }

        if (!inimigo.estaVivo()) {
            numInimigosDerrotados++;
            alert(`Você derrotou o ${inimigo.nome}!`);
        }

        const continuar = prompt('Deseja continuar o jogo? (s/n)');
        if (continuar.toLowerCase() !== 's') {
            break;
        }
    }

    alert(`Fim do jogo! Você derrotou ${numInimigosDerrotados} inimigos.`);
}

// Iniciar o jogo quando a página carrega
window.onload = iniciarJogo;
